# HydraDB's store/cache/WAL now live in a named Docker volume (hydra_data),
# not a host bind mount -- see docker-compose.yml. This directory may still
# appear locally from earlier debugging; harmless to ignore either way.
hydradb-data/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Ingest working data
.adit/
node_modules/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
